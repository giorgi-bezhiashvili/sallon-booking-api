# Host/port of a running clamd (ClamAV daemon). Uploaded photos are
# streamed to it and rejected if not reported clean.
CLAMD_HOST=127.0.0.1
CLAMD_PORT=3310

# Where scanned, clean photos get written on disk. Paths saved in the DB
# are relative to this directory.
UPLOAD_DIR=./uploads
